[workspace]
members = ["contracts/auction"]
resolver = "2"

# Optimize for contract size on release builds
[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true
